name: gg_one_merge
description: Merge flow machinery of the gg_one tool family - merging feature branches via pull requests or local merges.
version: 1.0.0
repository: https://github.com/ggsuite/gg_one_merge.git

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
